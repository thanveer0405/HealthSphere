name: demo
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  charts_flutter: ^0.12.0

  # ✅ Unified latest compatible Firebase versions
  firebase_core: ^3.15.2
  firebase_auth: ^5.7.0
  cloud_firestore: 5.6.12
  firebase_database: ^11.0.2
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  provider: ^6.1.5+1
  google_fonts: ^6.3.2
  flutter_animate: ^4.5.2
  firebase_messaging: ^15.2.10
  flutter_local_notifications: ^19.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
